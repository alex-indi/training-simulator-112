# UT112-3 — Bootstrap приложения

- **Branch:** `chore/UT112-3-bootstrap-app`
- **Owner:** Александр Гордеев
- **WEEEK:** `Готово`
- **Updated:** 2026-09-21 21:29 MSK

## Цель

Создать минимальный рабочий каркас проекта: FastAPI backend с `GET /health` и React + Vite frontend со стартовым экраном тренажёра.

## Сделано

- Пройден Git-gate и создана отдельная ветка задачи.
- Критерии задачи восстановлены из первоначального backlog (исторический документ доступен в Git до UT112-43).
- Создан FastAPI backend с системным endpoint `GET /health`.
- Добавлен backend-тест, Ruff-конфигурация и воспроизводимый `uv.lock`.
- Создан React + Vite frontend со стартовым экраном тренажёра.
- Добавлены ESLint-конфигурация и воспроизводимый `package-lock.json`.
- README дополнен командами установки, запуска и проверок.
- `.env.example` дополнен локальным URL API; `.env`, зависимости и сборки проверены как игнорируемые Git.
- Добавлен единый кроссплатформенный запуск backend и frontend через `dev.py`.

## Осталось

- В рамках UT112-3 — ничего.

## Проверки

- `uv run pytest` — 1 passed (2 deprecation-warning внутри FastAPI/Starlette).
- `uv run ruff check .` — passed.
- `npm run lint` — passed.
- `npm run build` — passed.
- `python3 dev.py` — одновременно запускает оба сервиса; backend и frontend отвечают по HTTP.
- `Ctrl+C` — штатно останавливает оба процесса и возвращает код `0`.
- `test_dev.py` — regression-тест единого shutdown-flow passed.
- Uvicorn запускался локально; `GET /health` вернул `200 OK` и ожидаемый JSON.
- Vite dev server запускался локально и отдал стартовую страницу.
- `git diff --check` — passed.

## Блокеры

- нет

## Следующий шаг

**Один конкретный шаг:** влить `chore/UT112-3-bootstrap-app` в `main` по принятому командой процессу.

## Последний checkpoint

- Commit: `b96f00d chore(UT112-3): добавить bootstrap приложения`
- Pushed: yes
