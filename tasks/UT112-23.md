# UT112-23 — Стабилизация интеграции

- **Branch:** `fix/UT112-23-main-stabilization`
- **Owner:** Александр Гордеев
- **WEEEK:** задача 1346, «В работе»
- **Updated:** 2026-09-24

## Цель

Исправить дефекты интеграции после UT112-17–19: инварианты занятия и карточек, realtime, отображение данных, LAN-запуск, polling, тесты и документацию. Описание и критерии — в задаче WEEEK и приложенном `UT112-023.md`.

## Сделано

- Задача назначена текущему пользователю и переведена в «В работе».
- `main` обновлён до `9823f8d` (коммит исходного аудита); создана отдельная ветка.
- Подтверждено, что UT112-20 пока не входит в `main`.
- Исправлены групповые назначения: параметры группы копируются в run, несовместимые override отклоняются; frontend следует новому контракту.
- Для `FIXED_SET` проверяется утверждённая карточка каждой индивидуальной или общей очереди.
- `READY` сбрасывается в `DRAFT` при изменении группы, массовом назначении и изменениях пула.
- Terminal DDS action завершает lifecycle с серверным временем; action до открытия и open вне активного занятия отклоняются.
- Realtime-события доставки, открытия, claim, действий ДДС и назначений групп используют единый publish helper; клиент перечитывает REST.
- API карточки возвращает профиль ДДС и АРМ текущего пользователя; UI не подставляет чужую ДДС или вымышленные факты.
- LAN-параметры перенесены в конфигурацию; Vite читает корневой `.env`.
- Heartbeat отвечает `204`; список занятий обучаемого возвращает лёгкий summary.
- Исправлены валидация пустых строк, повторное назначение ResponseUnit и время сгенерированной карточки.
- Добавлены CI, backend-регрессии, PostgreSQL concurrency test и три Playwright smoke-сценария.
- Актуализированы README и docs, исправлены устаревшие handoff UT112-17/19.

## Осталось

- Пользователю вручную проверить интерфейс на другом ПК в той же LAN и подтвердить завершение; только после этого переводить WEEEK в «Готово».

## Проверки

- Git-gate: текущая ветка содержит `UT112-23`, рабочее дерево до начала правок чистое.
- Ruff: успешно; pytest: 68 passed, 1 skipped (опциональный PostgreSQL-тест).
- PostgreSQL concurrency: 1 passed на отдельной БД.
- Alembic `upgrade head` и `check`: успешно, новых upgrade operations нет.
- Frontend lint/build: успешно; Playwright: 3 passed на отдельной БД.
- LAN smoke: frontend и `/health` доступны через LAN IP, Socket.IO handshake отвечает 200 с разрешённым origin.
- `git diff --check`: успешно.

## Блокеры

- Для `ResponseAssignment.CANCELLED` в UI требуется продуктовое решение; это не блокирует стабилизацию backend.

## Следующий шаг

**Один конкретный шаг:** пользователь открывает тренажёр с другого ПК в той же LAN и проверяет путь обучаемого и преподавателя.

## Последний checkpoint

- Реализация: `861090d`.
- Pushed: yes, ветка `fix/UT112-23-main-stabilization`.
