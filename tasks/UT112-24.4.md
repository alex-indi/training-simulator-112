# UT112-24.4 — Импорт медицинских объектов Москвы MVP

- **Branch:** `feature/UT112-24.4-medical-objects`
- **Owner:** Александр Гордеев
- **WEEEK:** 1351, «В работе»
- **Updated:** 2026-09-24

## Цель

Расширить Object Registry медицинскими объектами из data.mos.ru: больницами, поликлиниками и станциями скорой помощи. Сохранить идентификаторы и качество исходных данных, обеспечить повторяемый импорт.

## Сделано

- Задача назначена и переведена в работу; создана отдельная ветка на базе UT112-24.3.
- Проверена существующая схема и загрузчик Object Registry.
- В стартовые данные типов добавлен `EMERGENCY_STATION` как потомок `HEALTHCARE`; обновлены документация и проверка дерева типов.
- Ключ API сохранён в локальном игнорируемом Git файле `.env` с правами `0600`; клиент импорта теперь читает его из этого файла, если переменная процесса не задана.
- Добавлены ручные команды проверки API и выгрузки любого набора по ID; порядок запуска описан в исторической инструкции (актуальные команды в `docs/object-registry.md`).
- Убрана сортировка `$orderby=Number` из запроса строк: API набора 502 отвечал на неё HTTP 400. Добавлена проверка параметров пагинации.
- Получены пять официальных наборов: 502, 517, 503, 505, 516; паспорта и отчёты совпадают с выгруженными строками.
- Добавлен mapper медицинских объектов по физическим адресам, собран seed с сохранением исходных ID, адресов, типов и тегов; опубликован отчёт качества (его действующие ограничения перенесены в [документ реестра](../docs/object-registry.md)).
- Seed загружен в локальный PostgreSQL: 888 медицинских объектов, 4638 объектов всего. Повторная загрузка не изменила количества и не создала дублей.

## Осталось

- Нет обязательной работы по UT112-24.4. Координаты в полученных медицинских строках отсутствуют; это ограничение источника зафиксировано в отчёте качества.

## Проверки

- `UV_CACHE_DIR=/private/tmp/uv-cache-ut112-24-4 uv run --offline pytest -q tests/test_city_object_import.py tests/test_object_registry.py`: 9 passed после исправления HTTP 400.
- `UV_CACHE_DIR=/private/tmp/uv-cache-ut112-24-4 uv run --offline ruff check scripts/import_city_objects/mos_api_client.py tests/test_city_object_import.py tests/test_object_registry.py`: без ошибок.
- `git diff --check`: без ошибок.
- `import_objects build`: 888 медицинских объектов, 4638 объектов в общем seed.
- `import_objects load` и повторный запуск в локальном PostgreSQL: 888 медицинских объектов до и после, 0 дублей.
- Полный `pytest -q`: 56 passed; `ruff check .`: без ошибок.

## Блокеры

- Нет.

## Следующий шаг

**Один конкретный шаг:** использовать медицинские объекты реестра в следующей задаче генератора сценариев; UT112-24.4 завершена.

## Последний checkpoint

- Commit: см. `git log -1` текущей ветки.
- Pushed: yes.
