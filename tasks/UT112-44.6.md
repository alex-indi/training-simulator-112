# UT112-44.6 — Перегенерация текста карточки

## Что делаем

Исправляем повторную генерацию текста карточки и отображение результата, когда модель не дала новой формулировки.

## Что сделано

- Проверено на текущей БД: модель `gpt-6-luna` активна, прямой запрос отвечает; последние карточки подготовлены через OpenAI.
- Повторная генерация теперь передаёт предыдущий текст как контекст и просит иную формулировку без изменения фактов.
- Если провайдер не ответил или вернул прежний текст, API сообщает причину вместо ложного успеха.

## Что осталось

- Визуально проверить интерфейс позже вместе с общей UI-приёмкой.

## Проверки

- Прямой read-only запрос к настроенной модели: успешен.
- `pytest -q tests/test_text_generation.py tests/test_scenario_instances.py`: 7 passed.
- Ruff для изменённых backend-файлов: пройден.
- `git diff --check`: пройден.

## Блокеры

Локальный commit сохранён; push не выполнялся.

## Следующий шаг

При следующей визуальной приёмке проверить, что повторная генерация меняет текст и показывает ошибку при неизменном ответе модели.

## Последний commit / push

Нет.
