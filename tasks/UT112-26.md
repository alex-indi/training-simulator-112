# UT112-26 — Генерация экземпляра сценария из библиотеки

- **Branch:** `feature/UT112-26-scenario-instance`
- **Owner:** Александр Гордеев
- **WEEEK:** 1354, «Готово» после проверки 24.09.2026
- **Updated:** 2026-09-24

## Цель

Материализовать READY `ScenarioTemplate` в воспроизводимый `ScenarioInstance` с копиями данных классификатора, объекта, служб, событий и критериев, а также дать преподавателю предпросмотр и привязку к занятию.

## Сделано

- Задача назначена и переведена в «В работе».
- Создана отдельная ветка поверх UT112-25.
- Добавлены `ScenarioInstance` и `ScenarioInstanceEvent` с Alembic-миграцией.
- Реализованы предпросмотр, детерминированный выбор объекта, генерация snapshot, чтение, привязка и список по занятию. Полный экземпляр недоступен обучаемому.
- Добавлен преподавательский путь в библиотеке и список привязанных экземпляров в подготовке занятия.
- Обновлены концепция генератора, Scenario Engine и архитектура.
- На отдельной PostgreSQL-базе применены все миграции, загружены CORE/DEMO данные; выполнены API и браузерная проверки на реальных справочниках.

## Осталось

- Восстановить доступ к GitHub и отправить ветку. Код и проверки сохранены локальным commit.

## Проверки

- Backend `pytest -q`: 99 passed, 3 skipped.
- Ruff, frontend ESLint и Vite build: успешно.
- Playwright UT112-26 с мокированным API: 1 passed.
- Alembic: SQL для миграции `20260924_18:20260924_19` успешно сформирован.
- PostgreSQL: чистая изолированная база `ut112_26_verify_20260924_a1`, миграции до `20260924_19` и `bootstrap_demo` успешны; 1283 правила SRC-006, 209 служб, 4638 объектов.
- Реальный API: preview → выбор объекта → создание экземпляра → привязка к занятию → чтение списка; обучаемому доступ запрещён (403).
- Snapshot после изменения исходного объекта, правила SRC-006 и шаблона остался прежним; тестовые исходные данные восстановлены.
- Playwright UT112-26 на реальном backend/PostgreSQL: 1 passed.
- Повторно: backend `pytest -q` — 99 passed, 3 skipped; Ruff, ESLint, Vite build — успешно.

## Блокеры

- Push через SSH не прошёл: соединение с GitHub закрыто; `gh auth status` сообщает о недействительном токене. Это не влияет на локальную проверку, но ветка пока не доступна на другом компьютере.

## Следующий шаг

Восстановить авторизацию GitHub и выполнить `git push -u origin feature/UT112-26-scenario-instance`.

## Последний checkpoint

- Commit: `feat(UT112-26): generate scenario instances from library` (локально).
- Pushed: нет, SSH-соединение с GitHub закрыто удалённой стороной.
