# UT112-31.1 — Безопасность материалов заказчика

## Что делаем

Исключаем исходные материалы заказчика из Git, проверяем архив, историю и возможные секреты. Переписывание истории — отдельная операция после решения владельца репозитория.

## Сделано

- Создана подзадача WEEEK 1361 в рамках UT112-33; родительская задача и подзадача назначены текущему пользователю и переведены в «В работе».
- Создана ветка `feature/UT112-31.1-customer-materials-security` от актуального `main` (`082a7f5`).
- Подтверждено, что `materials/customer.zip` отслеживается Git, а `.gitignore` пока исключает только `materials/customer/`.
- Проверены 13 основных документов архива, текстовые Git blobs и история; найденные признаки и ограничения записаны в `docs/security/customer-materials-audit.md` без раскрытия содержимого.
- Архив удалён из индекса Git без удаления локальной копии; `.gitignore` исключает `materials/customer/` и `materials/customer.*`.
- Обновлено правило хранения источников в `docs/source-data-analysis.md`.

## Осталось

- Проверить diff и правила игнорирования; затем выполнить commit, push, PR и дождаться CI.
- Отдельное решение владельца репозитория по history rewrite. Оно не входит в этот PR.

## Проверки

- Текстовое сканирование архива, HEAD и истории проведено; ограничения описаны в отчёте.
- `git diff --check` и `git diff --cached --check` — успешно.
- `git check-ignore` подтверждает исключение архива и каталога; `git ls-files --cached --error-unmatch materials/customer.zip` возвращает 1 (архив отсутствует в индексе).
- Backend: `ruff check .` — успешно; `pytest -q` — 100 passed, 4 skipped.
- Frontend: `npm run lint`, `npm run build` — успешно.
- CI ожидается после PR.

## Блокеры

- Полная проверка изображений и сканов требует OCR/ручного просмотра. Для history rewrite нужно отдельное решение владельца.

## Следующий шаг

Выполнить Git-проверки индекса и diff, затем commit, push и PR для текущего дерева без customer archive.

## Последний commit / push

- Ветка создана от `082a7f5`; commit и push изменений задачи ещё не выполнены.
