# UT112-31.3 — Адресация сценарных сообщений конкретной службе

## Что делаем

Для каждого `RESPONSE_MESSAGE` задаём службу-источник в шаблоне, сохраняем её в snapshot экземпляра и доставляем сообщение только назначению этой службы. Событие остаётся `PENDING`, пока назначение не создано.

## Сделано

- Создана подзадача WEEEK 1363, назначена текущему пользователю и переведена в «В работе».
- Создана ветка `feature/UT112-31.3-response-message-targeting` от очищенного `main` `9671f93` в изолированном clone.
- Добавлены адресат `RESPONSE_MESSAGE`, проверка готовности шаблона, snapshot экземпляра и миграция старых данных с однозначной службой.
- Назначение виртуальной группы связывается со службой сценария; событие ждёт назначения и затем отправляется ровно один раз в нужный чат.
- Добавлены выбор службы в интерфейсе шаблона и карточки, обновлены seed и документация.

## Осталось

- Проверить поведение вручную и получить подтверждение пользователя; после этого слить PR #24 и перевести задачу WEEEK в «Готово».

## Проверки

- `git branch --show-current` содержит `UT112-31.3`.
- История нового clone не содержит `materials/customer.zip` и `materials/customer/`.
- Backend после merge с `main` `3140724`: 128 тестов прошли, 4 пропущены; интеграционный тест сценарного runtime на PostgreSQL прошёл.
- Frontend: ESLint и Vite build прошли.
- `alembic heads`: одна вершина `20260925_074331_b9d594`.
- `alembic upgrade head` успешно применён на чистой временной PostgreSQL базе.
- После merge с `main` цепочка миграций перестроена через `20260925_071858_c7748e`; `alembic upgrade head` снова прошёл на новой чистой PostgreSQL базе.
- Ruff и `git diff --check` прошли.
- PR #24 после merge с `main`: `MERGEABLE`, состояние `CLEAN`, все backend/frontend CI-проверки прошли.

## Блокеры

- Нет.

## Следующий шаг

Передать PR #24 пользователю для проверки поведения и дождаться решения о слиянии.

## Последний commit / push

- Код: `a4f5773`; merge с `main`: `a835efa`; PR #24 открыт, ветка опубликована.
