# UT112-DB-001 — Воспроизводимое наполнение PostgreSQL

- **Branch:** `feature/UT112-DB-001-reproducible-seeds`
- **Owner:** Александр Гордеев
- **WEEEK:** 1358, «В работе»
- **Updated:** 2026-09-24

## Цель

Восстанавливать обязательные и демонстрационные данные из версионированных файлов поверх схемы Alembic, не затрагивая историю тренировок.

## Сделано

- Задача назначена и переведена в работу; создан отдельный worktree из `origin/main`.
- Добавлены versioned DEMO JSON, устойчивый `seed_code` виртуальной группы, идемпотентный `seed_demo`.
- Добавлены точки входа `seed_core`, `seed_all`, `bootstrap_demo` и строгая проверка обязательных CORE-таблиц.
- Обновлены README, AGENTS и архитектурная документация.

## Осталось

- Совместить миграции UT112-24.1/24.3 с актуальным `main` и подключить CORE-импортёр реестра объектов.
- Добавить сценарии после появления `ScenarioTemplate`, source manifests и полный fresh-DB CI smoke.
- Проверить `seed_all` на чистой PostgreSQL после объединения зависимостей.

## Проверки

- Alembic upgrade из чистой временной PostgreSQL на `127.0.0.1:55432` прошёл до `20260924_12`.
- Повторный `seed_demo` не создал дублей; изменение записи восстановлено следующим запуском.
- Тест на PostgreSQL проверил идемпотентность, обновление и сохранность `TrainingSession`.
- Полный backend pytest: 83 passed, 2 skipped; отдельный PostgreSQL seed-тест: 2 passed; Ruff и `git diff --check` без ошибок.

## Блокеры

- На текущем `main` ещё нет классификатора, реестра объектов и `ScenarioTemplate`.
- Пробное слияние UT112-24.3 с актуальным `main` отменено: обе ветки используют Alembic revision `20260924_10`, а модель сообщений конфликтует. В UT112-24.3 ещё отсутствует готовый импортёр нормализованного Object Registry.

## Следующий шаг

**Один конкретный шаг:** после совместимого объединения UT112-24.1/24.3 с `main` добавить `seed.object_registry.import_seed` для JSON в `backend/seed/city_objects/` и подключить его к `seed_core`.

## Последний checkpoint

- Commit: `24931c6` (основа seed/bootstrap); финальные правки ожидают коммита
- Pushed: no
