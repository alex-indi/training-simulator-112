# UT112-4 — PostgreSQL и миграции

- **Branch:** `feature/UT112-4-postgresql-migrations`
- **Owner:** Александр Гордеев
- **WEEEK:** `В работе`
- **Updated:** 2026-09-22 09:05 MSK

## Цель

Подключить PostgreSQL как основное хранилище backend и настроить SQLAlchemy 2 + Alembic без хранения секретов в Git.

## Сделано

- Задача назначена Александру Гордееву и переведена в `В работе`.
- Пройден Git-gate и создана отдельная ветка задачи.
- После обновления remote-ссылок ветка перенесена на актуальный `origin/main` с уже слитой UT112-3.
- Прочитаны критерии WEEEK и актуальная архитектура проекта.
- Добавлена environment-конфигурация `DATABASE_URL` через `pydantic-settings`.
- Настроены SQLAlchemy 2, асинхронный `asyncpg` engine и фабрика сессий.
- Backend проверяет PostgreSQL при старте, логирует успешное соединение и integration failure без вывода URL/секретов.
- Настроен Alembic с асинхронным online-режимом и offline-генерацией SQL.
- Добавлена начальная baseline-миграция `20260921_01`.
- Обновлены `.env.example`, README и lockfile зависимостей.
- Добавлены тесты конфигурации, engine/session factory и startup lifecycle.
- Найдена причина сбоя Homebrew: HTTP/2 stream error при загрузке bottle из GitHub Container Registry. Повторная установка с HTTP/1.1 завершилась успешно.
- Установлен PostgreSQL 16.15, запущен локальный сервис и создана база `training_simulator_112`.
- Online-миграция успешно применена: в `alembic_version` записана `20260921_01`.
- FastAPI запущен с `DATABASE_URL` из окружения: подключение к PostgreSQL подтверждено логом, `GET /health` вернул `200 OK`.

## Осталось

- Дождаться проверки результата пользователем перед переводом WEEEK-задачи в `Готово`.

## Проверки

- `uv run ruff check .` — passed.
- `uv run pytest` — 5 passed, 2 сторонних deprecation warnings FastAPI/Starlette.
- `uv run alembic heads` — `20260921_01 (head)`.
- `uv run alembic upgrade head --sql` — сформировал транзакционный SQL и запись версии `20260921_01`.
- Offline Alembic проверен с URL-encoded паролем (`%40`).
- `git diff --check` — passed.
- `.env` подтверждён как игнорируемый Git.
- Попытка установить PostgreSQL 16 через Homebrew начата, но не завершилась из-за ошибки загрузки HTTP/2 из GitHub Container Registry; формула не установлена.
- PostgreSQL 16.15: `pg_isready` — accepting connections.
- `DATABASE_URL=... uv run alembic upgrade head` — online migration passed; таблица `alembic_version` содержит `20260921_01`.
- `DATABASE_URL=... uv run uvicorn app.main:app --port 8011` — startup passed; лог подтверждает подключение к PostgreSQL.
- `GET http://127.0.0.1:8011/health` — `200 OK`.

## Блокеры

- Нет.

## Следующий шаг

**Один конкретный шаг:** получить явное подтверждение пользователя перед переводом UT112-4 в `Готово`.

## Последний checkpoint

- Commit: `c0b1d6e chore(UT112-4): сохранить промежуточное состояние`
- Pushed: yes
