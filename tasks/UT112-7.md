# UT112-7 — Базовая карточка происшествия 112

- **Branch:** `feature/UT112-7-basic-incident`
- **Owner:** Александр Гордеев
- **WEEEK:** `Готово`
- **Updated:** 2026-09-22 MSK

## Цель

Добавить сохраняемую карточку `Incident`, связанную с учебной сессией, со snapshot исходных данных сценария, серверными временными метками и REST API чтения.

## Сделано

- Задача назначена Александру Гордееву и переведена в `В работе`.
- Пройден Git-gate и создана отдельная ветка поверх завершённой зависимости UT112-6.
- Прочитаны критерии WEEEK, текущее состояние, архитектура, предметная модель и lifecycle карточки.
- Добавлен модуль `incidents`: модель `Incident`, enum lifecycle и связь с `TrainingSession`.
- Добавлены типизированные данные карточки и сохраняемый JSONB snapshot с поддержкой дополнительных полей сценария.
- Доставка карточки фиксирует `created_at` и `delivered_at` на backend; `opened_at` и `primary_status_at` подготовлены для следующих задач.
- Добавлены REST API доставки, списка и чтения карточки с разграничением доступа по учебной сессии.
- Добавлена миграция `20260922_03`, тесты и описание API в README.
- Выполнен финальный review: проверены все критерии WEEEK и весь относящийся diff.
- Реализация сохранена в Git и отправлена в `origin`.

## Осталось

- Нет — задача завершена.

## Проверки

- `uv run ruff check .` — passed.
- `uv run pytest` — 20 passed, 2 сторонних deprecation warnings FastAPI/Starlette.
- `uv run alembic heads` — `20260922_03 (head)`.
- `uv run alembic upgrade head --sql` — SQL всей цепочки миграций сформирован.
- Online `uv run alembic upgrade head` — passed на локальной PostgreSQL.
- Persistence smoke-test — карточка и дополнительные поля snapshot восстановлены после повторного чтения; временные данные удалены.
- `git diff --check` — passed.

## Блокеры

- Нет.

## Следующий шаг

**Один конкретный шаг:** нет, задача завершена.

## Последний commit / push

- Implementation commit: `e3a1051 feat(UT112-7): добавить базовую карточку происшествия`.
- Pushed: yes.
