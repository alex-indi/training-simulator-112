# UT112-27.1 — AI Text Renderer

## Что делаем
Готовим русскоязычные тексты карточки и сообщений службы из проверенных фактов до занятия, сохраняем их в ScenarioInstance и поддерживаем смену AI-провайдера конфигурацией.

## Уже сделано
- Задача назначена Александру Гордееву и переведена в «В работе» в WEEEK.
- Создана ветка `feature/UT112-27.1-ai-text-renderer` от актуального `main`.
- Реализованы конфигурация, провайдеры OpenAI/OpenAI-compatible, healthcheck, таймаут/повтор и шаблонный резерв.
- Исходная карточка и сообщения группы получают отдельные сохранённые тексты и metadata при создании экземпляра; runtime использует сохранённый текст.
- Добавлены черновик экземпляра, просмотр фактов/текста, ручная правка, явная перегенерация, подтверждение и повторное открытие черновика в UI.
- Обновлены миграция статуса, примеры env, архитектурная документация и проверки.

## Осталось
- Проверить на пилотном стенде с реальным настроенным провайдером и дать задачу на пользовательскую приёмку.

## Проверки
- Backend: `124 passed, 5 skipped` (включая opt-in live AI test).
- Ruff для backend: без замечаний; frontend build и ESLint: успешно; 6 frontend unit tests: успешно.
- `alembic upgrade head` на пустой временной PostgreSQL: `20260925_071858_c7748e (head)`.
- `git diff --check`: без замечаний.
- Реальный OpenAI Models API: HTTP 200, модель `gpt-5.6-luna` доступна; healthcheck: `AVAILABLE`; `RESPONSE_MESSAGE` получен через OpenAI без fallback (96 input / 11 output tokens). Ключ не выводился.
- Live `INCIDENT_REPORT` на разрешённом пользователем полностью вымышленном примере: OpenAI ответил без fallback (144 input / 103 output tokens). Первый вариант с конкретной школой и адресом был отклонён автоматической проверкой и не отправлялся.

## Блокеры
Нет. Локальный `.env` не изменён; включение renderer для реальных карточек требует отдельного решения при пилотном запуске.

## Следующий шаг
Выполнить smoke-сценарий создания, правки и подтверждения экземпляра на пилотном стенде перед пользовательской приёмкой.

## Последний commit / push
`feat(UT112-27.1): add provider agnostic AI text rendering` в `feature/UT112-27.1-ai-text-renderer`; ветка отправлена в `origin`.
