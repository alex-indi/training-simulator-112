# UT112-41 — исправить локальный запуск после обновления

## Что делаем

Восстановить согласованную историю миграций локальной PostgreSQL без удаления данных и добиться запуска `python dev.py` без ошибок схемы. Зафиксировать способ восстановления для других компьютеров.

## Сделано

- Создана задача WEEEK 1367, назначена текущему разработчику, статус «В работе».
- Ветка `fix/UT112-41-legacy-migration-startup` создана от актуального `main`.
- Воспроизведена ошибка `UndefinedColumnError: training_sessions.paused_at`.
- Установлено: Alembic показывает `20260924_18`, но таблицы и поля миграции `20260924_10` отсутствуют.
- Добавлен `prepare_dev_database`: он распознаёт известную старую схему, одной транзакцией применяет пропущенные миграции `10`–`12`, затем обновляет базу до `head`.
- `dev.py` вызывает миграции до запуска backend/frontend; README и руководство по данным обновлены.
- Локальная БД восстановлена до `20260925_145915_663d54` с сохранением рабочих данных.

## Осталось

- Получить подтверждение пользователя после проверки запуска на его рабочем месте.

## Проверки

- `python dev.py` — backend запускается, но планировщик повторно сообщает об отсутствующей колонке.
- `alembic heads` — одна вершина `20260925_145915_663d54`.
- `alembic current` — `20260924_18`.
- Повторный `python dev.py` — backend запустился без ошибки схемы, frontend сообщил `VITE ready`.
- `GET /health` — 200 и `status=ok`; frontend `/` — HTTP 200.
- `alembic current` — `20260925_145915_663d54 (head)`.
- `pytest tests/test_dev.py -q` — 2 passed; `ruff check` и `git diff --check` — без ошибок.
- `alembic check` выявил отдельный старый дрейф индексов `object_tag_classifier_features` и `user_groups`; на запуск не влияет.

## Блокеры

Нет для запуска. Исправление дрейфа индексов относится к отдельной задаче.

## Следующий шаг

Попросить пользователя проверить обычный `python dev.py` на его рабочем месте и подтвердить завершение UT112-41.

## Последний commit / push

Изменения UT112-41 ещё не коммичены и не отправлены.
