# UT112-41 — Актуализация состояния проекта и правил библиотеки

## Цель

Синхронизировать проектную память с фактическим `main` после последних merge и зафиксировать продуктовое правило архивирования сценариев.

## Сделано

- Создана ветка `chore/UT112-41-current-state` от актуального `main`.
- Полностью актуализирован `docs/current-state.md` по состоянию на 26.09.2026 и HEAD `f57f4dc`.
- Зафиксирована фактическая граница MVP, текущее состояние сценарного workflow, AI, Object Registry, админки, преподавательского кабинета, CI и оставшихся P0/P1 работ.
- Зафиксировано правило: черновики могут совместно исправлять преподаватели и администраторы; READY-сценарий архивирует только его автор или ADMIN.
- В backend добавлена проверка этого правила в `/api/scenario-templates/{id}/archive`.
- Добавлены unit-тесты: автор может архивировать свой сценарий, другой преподаватель не может, ADMIN может архивировать любой.

## Проверки

Ожидаемые обязательные проверки перед merge:

```text
backend: uv run ruff check .
backend: uv run pytest -q
frontend: npm run lint
frontend: npm run build
bootstrap_demo на чистой PostgreSQL
```

## Блокеры

Нет.

## Следующий шаг

Открыть PR, дождаться зелёного CI и после merge перейти к сквозной приёмке MVP на чистой базе.
