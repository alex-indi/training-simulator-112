# UT112-41 — исправить локальный запуск после обновления

## Что делаем

Восстановить локальную PostgreSQL и запускать миграции репозитория автоматически перед сервисами через `python dev.py`.

## Сделано

- Создана задача WEEEK 1367, назначена текущему разработчику, статус «В работе».
- Ветка `fix/UT112-41-legacy-migration-startup` создана от актуального `main`.
- Воспроизведена ошибка `UndefinedColumnError: training_sessions.paused_at`.
- Установлено: запись Alembic опережала фактическую схему локальной БД после параллельных веток.
- Локальная БД восстановлена разовым применением пропущенных миграций через Alembic с сохранением рабочих данных.
- `dev.py` вызывает обычный `alembic upgrade head` до запуска backend/frontend. В штатном запуске нет привязки к конкретным revision ID.
- `/api/admin/system` читает текущую ревизию из `alembic_version`, а не показывает фиксированное значение.
- README и руководство по данным обновлены для общего порядка миграций.

## Осталось

- Получить подтверждение пользователя после проверки запуска на его рабочем месте.

## Проверки

- Исходная ошибка `python dev.py` воспроизведена до восстановления БД.
- После упрощения `python dev.py --backend-port 18080 --frontend-port 15173` запустил backend без ошибки схемы; frontend сообщил `VITE ready`. Обычные порты уже были заняты другим запущенным экземпляром.
- `GET /health` — 200 и `status=ok`; frontend `/` — HTTP 200.
- Вызов `/api/admin/system` через сессию вернул фактическую ревизию из `alembic_version`.
- `alembic current` — `head`.
- `pytest tests/test_dev.py tests/test_admin.py -q` — 33 passed; `ruff check` и `git diff --check` — без ошибок.
- `alembic check` выявил отдельный старый дрейф индексов `object_tag_classifier_features` и `user_groups`; на запуск не влияет.

## Блокеры

Нет для запуска. Исправление дрейфа индексов относится к отдельной задаче.

## Следующий шаг

Попросить пользователя проверить обычный `python dev.py` на его рабочем месте и подтвердить завершение UT112-41.

## Последний commit / push

`a651b56` — универсальный запуск миграций; отправлен в `origin/fix/UT112-41-legacy-migration-startup`.
