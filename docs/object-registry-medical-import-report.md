# Импорт медицинских объектов Москвы — UT112-24.4

Источник — исходные выгрузки data.mos.ru в `backend/seed/object_registry/source_data/`, полученные 24.09.2026. В каждом наборе паспорт, строки и отчёт выгрузки согласованы по ID и числу строк. Генерация seed выполняется командой `cd backend && uv run python -m scripts.import_city_objects.import_objects build`, загрузка — командой `uv run python -m scripts.import_city_objects.import_objects load`.

| Набор | Назначение | Строк | Объектов по адресам | Без координат |
| --- | --- | ---: | ---: | ---: |
| 502 | Детские больницы | 14 | 66 | 66 |
| 517 | Взрослые больницы | 37 | 189 | 189 |
| 503 | Взрослые поликлиники | 260 | 279 | 279 |
| 505 | Детские поликлиники | 201 | 212 | 212 |
| 516 | Скорая и неотложная помощь | 76 | 143 | 143 |
| **Итого** | | **588** | **888** | **888** |

В наборе 517 одна закрытая организация с пятью адресами исключена из seed. У 31 строки статус закрытия не указан; они включены, а пустой статус сохранён в атрибуте `close_flag`. Идентификаторы исходной строки и адреса сохранены в `external_id` и атрибутах `source_row_id`, `source_address_id`. Адрес, район и административный округ берутся из каждой записи `ObjectAddress`.

Все полученные медицинские строки содержат пустые `geoData` и `geodata_center`. Координаты в `city_objects` оставлены `NULL`: геокодирование по тексту адреса не выполнялось. `24_hours` ставится только при круглосуточном графике на все семь дней; поликлиникам с другим графиком ставится `daytime`.

Проверка в локальном PostgreSQL: после загрузки 888 медицинских объектов общее число объектов реестра — 4638. Повторная загрузка сохранила оба числа и не создала дублей `(source, external_id)`.
