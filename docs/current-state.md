# Текущее состояние `main`

**Дата:** 24.09.2026
**HEAD при актуализации:** `9823f8d` (merge UT112-19)

## Продукт и граница MVP

Основной обучаемый — диспетчер ДДС. Он получает готовую карточку Системы-112, принимает решение своей службы, общается с виртуальной группой реагирования и сам устанавливает статусы. Состояние группы не меняет статус ДДС автоматически. Целевая модель кабинета преподавателя — [`instructor-workspace-concept.md`](instructor-workspace-concept.md).

## Реализовано в `main`

- FastAPI, React/Vite, PostgreSQL, SQLAlchemy/Alembic, локальные demo-пользователи и роли.
- `TrainingSession` с настройками занятия, режимами `FLOW`/`FIXED_SET`/`MANUAL`, состояниями `DRAFT`/`READY`/`ACTIVE`, преподавательским setup API.
- `TrainingRun` на обучаемого, АРМ, профиль ДДС, сложность и `TrainingGroup`.
- `TrainingScenario` и `ScenarioQueueItem`: подготовка, утверждение, случайный или последовательный порядок и автоматическая выдача по серверному времени.
- Индивидуальные и общие очереди, атомарный claim общей карточки одним `TrainingRun`.
- `Incident` с исходным snapshot, историей действий, статусами ДДС, серверными временными метками и восстановлением через REST.
- `ResponseUnit` и `ResponseAssignment` с отдельным состоянием и историей сценарных переходов.
- Socket.IO уведомления о выдаче, claim и части изменений; после reconnect клиент перечитывает REST.
- Единый локальный запуск через `dev.py`.

## Работа в ветках

- UT112-20 (live-монитор и оперативный чат) находится в отдельной ветке и ещё не входит в указанный `main`. При интеграции не откатывать его интерфейс.
- UT112-23 стабилизирует доменные инварианты, права, realtime, LAN, polling, тесты и документацию. Состояние незавершённой работы — [`tasks/UT112-23.md`](../tasks/UT112-23.md).
- UT112-21 выполняется в `feature/UT112-21-instructor-controls` поверх UT112-23 и UT112-20. Паузы, команды преподавателя и завершение находятся в этой ветке; состояние — [`tasks/UT112-21.md`](../tasks/UT112-21.md).
- UT112-22 выполняется в `feature/UT112-22-final-assessment` поверх UT112-21. Разбор, экспертная проверка и история находятся в этой ветке; состояние — [`tasks/UT112-22.md`](../tasks/UT112-22.md).
- UT112-27 выполняется в `feature/UT112-27-scenario-runtime` поверх UT112-26. Интеграция подготовленных экземпляров с очередью, Incident и событиями — [`tasks/UT112-27.md`](../tasks/UT112-27.md).

## Пока не реализовано в `main`

- Паузы и вмешательство преподавателя UT112-21.
- Итоговый разбор и экспертная оценка UT112-22.
- Полный RoutingEngine, телефония и AI-оценка.

## Открытые предметные вопросы

- Какие исходные поля карточки может редактировать ДДС и как исправлять ошибки источника.
- Какие конкретные ДДС войдут в MVP.
- Нужен ли `ResponseAssignment.CANCELLED` в UI; backend уже поддерживает отмену из незавершённого состояния.
- Точная формула итоговой оценки.
